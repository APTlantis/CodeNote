// Deprecated: superseded by ../blue-slate/Tabs.tsx, which implements the
// blue-slate starter-pack's class contract (atl-tabs/atl-tab/atl-tab-active)
// instead of this file's ad hoc cn-tab* classes.
//
// Kept only as a re-export since this session's tools can't delete files on
// disk — safe to delete src/components/layout/TabBar.tsx by hand.
export { Tabs as TabBar } from "../blue-slate/Tabs";
