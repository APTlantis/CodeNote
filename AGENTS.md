# CodeNote Agent Instructions

Inherit `D:\AGENTS.md`, then read `CodeNote.manifest.toml`, `Project-README.md`, `README.md`, and `SPEC.md`.

CodeNote is governed as a DRS desktop application. Preserve the narrow editor/preview/terminal scope unless City Hall or the project docs explicitly change it.

Do not expose local package caches, build output, or secrets in generated docs.
